# vim: ts=2
---

# Production setup

- hosts: all
  remote_user: vagrant
  become: yes
  become_method: sudo
    
  vars_files:
    -  ../../config_global_vars.yml
      
  tasks:
    - include_role:
        name: ../roles/backend

    - include_role:
        name: ../roles/frontend